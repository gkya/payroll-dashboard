@page
@model PayrollDashboard.Pages.ImportModel
@{
    ViewData["Title"] = "Import";
}

<div class="py-4">
    <div class="mb-4">
        <h1 class="display-5">Import Payroll PDF</h1>
        <p class="lead mb-0">給与明細 PDF を取り込むためのページです。まずは画面の土台から作っていきます。</p>
    </div>

    <div class="mb-4">
        <form method="post" asp-page-handler="ImportAll">
            <button class="btn btn-outline-secondary" type="submit">datas フォルダから一括取込</button>
        </form>
        @if (!string.IsNullOrWhiteSpace(Model.BulkMessage))
        {
            <div class="alert alert-secondary mt-2">@Model.BulkMessage</div>
        }
    </div>

    @if (!string.IsNullOrWhiteSpace(Model.StatusMessage))
    {
        <div class="alert alert-info" role="alert">
            @Model.StatusMessage
        </div>
    }

    @if (Model.CurrentSlip is not null)
    {
        <div class="card shadow-sm mb-4">
            <div class="card-body">
                <h2 class="h4 mb-3">Latest Imported Slip</h2>
                <dl class="row mb-0">
                    <dt class="col-sm-3">File Name</dt>
                    <dd class="col-sm-9">@Model.CurrentSlip.SourceFileName</dd>

                    <dt class="col-sm-3">Payroll Month</dt>
                    <dd class="col-sm-9">@Model.CurrentSlip.PayrollMonth</dd>

                    <dt class="col-sm-3">Imported At</dt>
                    <dd class="col-sm-9">@Model.CurrentSlip.ImportedAt.LocalDateTime.ToString("yyyy-MM-dd HH:mm:ss")</dd>
                </dl>
            </div>
        </div>
    }

    <div class="row g-4">
        <div class="col-lg-7">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h2 class="h4 mb-3">Upload Form</h2>
                    <p>今回は送信の流れまで作り、次のステップで保存処理を追加します。</p>

                    <form method="post" enctype="multipart/form-data">
                        <div class="mb-3">
                            <label class="form-label" for="uploadFile">Payroll PDF</label>
                            <input class="form-control" id="uploadFile" type="file" asp-for="UploadFile" accept=".pdf,application/pdf" />
                        </div>
                        <div class="mb-3">
                            <label class="form-label" for="payrollMonth">Payroll Month</label>
                            <input class="form-control" id="payrollMonth" type="text" asp-for="PayrollMonth" placeholder="2026-03" />
                        </div>
                        <button class="btn btn-primary" type="submit">Upload</button>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-lg-5">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <h2 class="h4 mb-3">What We Will Build</h2>
                    <ul class="mb-0">
                        <li>PDF を選択してアップロードする</li>
                        <li>支給月やファイル名を保存する</li>
                        <li>取込結果を一覧画面で確認する</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
